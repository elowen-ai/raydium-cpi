[toolchain]
anchor_version = "0.31.1"

[features]
seeds = false
skip-lint = false

[registry]
url = "https://github.com/lowen-ai/raydium-locking-cpi"

[provider]
cluster = "Localnet"
wallet = "/home/ubuntu/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
